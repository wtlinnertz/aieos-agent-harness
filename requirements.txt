# Development dependencies (minimum versions).
# For verified installs, use requirements-lock.txt.
pyyaml>=6.0
anthropic>=0.40
openai>=1.50
pytest>=8.0
# M2 CI/CD capability substrate
jsonschema[format]>=4.20
structlog>=24.0
